public class Solution {
    public boolean canJump(int[] nums) {
        for (int i = 0; i < nums.length; i++) {
            for (int ind = i; ind < i + nums[i]; ind++) {
                if (i + nums[i]
            }
        }
    }
}
